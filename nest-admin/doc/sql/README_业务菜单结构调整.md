# 业务菜单结构调整说明

## 📋 调整概述

将原本归属于"业务管理"父级容器下的所有子模块提升为顶级一级菜单，简化菜单层级结构。

## ✅ 已完成的调整

### 1. 数据库菜单结构调整

#### 提升为顶级菜单的模块
以下菜单已从 `parent_id = 25`（业务管理）调整为 `parent_id = NULL`（顶级菜单）：

| 菜单名称 | ID | 路径 | 排序 | 类型 |
|---------|-----|------|------|------|
| 项目管理 | 26 | projectManage | 4 | catalog |
| 任务管理 | 27 | taskManage | 5 | catalog |
| 工单管理 | 28 | ticketManage | 6 | catalog |
| 文档管理 | 29 | documentManage | 7 | catalog |
| CRM客户管理 | 150 | crm | 8 | catalog |

#### 表单菜单的父级调整
各模块的表单菜单已正确关联到对应的父菜单：

| 表单名称 | ID | 新父级ID | 路径 |
|---------|-----|---------|------|
| 项目表单 | 30 | 26 (项目管理) | projectManage/form |
| 任务表单 | 31 | 27 (任务管理) | taskManage/form |
| 工单表单 | 32 | 28 (工单管理) | ticketManage/form |
| 文档表单 | 33 | 29 (文档管理) | documentManage/form |

#### "业务管理"菜单处理
- **状态**: 保留但隐藏 (`is_hidden = 1`)
- **原因**: 保持数据完整性，避免删除可能导致的引用问题
- **影响**: 该菜单不会在侧边栏显示，但数据库记录仍然存在

### 2. 前端路由配置

前端使用动态路由加载，无需修改代码。路由转换函数 `transRouter` 会自动处理：
- 顶级菜单路径自动添加 `/` 前缀
- catalog 类型菜单正确使用 Layout 组件
- 子路由正确嵌套

### 3. SQL脚本更新

#### 更新的脚本文件
1. **insert_business_menus.sql** - 初始安装脚本
   - 移除了"业务管理"父级菜单的创建
   - 所有业务模块直接作为顶级菜单插入
   - 更新了排序值（4-8）

2. **promote_business_menus_to_top.sql** - 迁移脚本
   - 用于从旧结构迁移到新结构
   - 包含完整的菜单关系调整
   - 可重复执行（幂等性）

## 🔍 验证结果

### 当前顶级菜单列表（按排序）
```
1. 首页 (index)
1. 系统管理 (system)
2. 内容管理 (content)
2. 系统监控 (systemMonitor)
4. 项目管理 (projectManage)          ← 新增顶级
5. 任务管理 (taskManage)             ← 新增顶级
6. 工单管理 (ticketManage)           ← 新增顶级
7. 文档管理 (documentManage)         ← 新增顶级
8. CRM客户管理 (crm)                 ← 新增顶级
```

### 菜单层级示例
```
项目管理 (catalog, 顶级)
├── 项目列表 (自动重定向)
└── 项目表单 (menu, 隐藏)

任务管理 (catalog, 顶级)
├── 任务列表 (自动重定向)
└── 任务表单 (menu, 隐藏)

...其他模块同理
```

## 📝 权限配置

所有菜单的 `permissionKey` 保持不变：
- `business:project` - 项目管理
- `business:task` - 任务管理
- `business:ticket` - 工单管理
- `business:document` - 文档管理
- `business:project:edit` - 项目编辑权限
- 等等...

角色权限分配无需调整，因为 permissionKey 未改变。

## ⚠️ 注意事项

1. **清除缓存**: 调整后需要用户重新登录或清除浏览器缓存
2. **角色权限**: 确保相关角色已分配新顶级菜单的权限
3. **书签链接**: 如果有直接访问旧路径的书签，可能需要更新
4. **API路径**: 后端API路径不受影响，仍为 `/api/business/*`

## 🔄 回滚方案

如需回滚到旧结构，可以：
1. 恢复数据库备份
2. 或执行反向SQL（将 parent_id 改回 25）

## 📅 执行时间

- **数据库调整**: 已完成
- **SQL脚本更新**: 已完成
- **前端适配**: 无需修改（动态路由）
- **测试验证**: 待进行

---

**生成时间**: 2026-04-09  
**执行人**: AI Assistant  
**影响范围**: 菜单结构、用户界面导航
